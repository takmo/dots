# golangci-lint changed their command now now the default value no longer works
[language-server.golangci-lint-lsp.config]
# command = ["golangci-lint", "run", "--output.json.path=stdout", "--show-stats=false", "--issues-exit-code=1"]
command = ["golangci-lint", "run", "--show-stats=false", "--issues-exit-code=1"]
